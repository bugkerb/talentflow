# UX/UI QA Loop — 2026-08-21

## Scope

ตรวจหน้า Dashboard, ตำแหน่งงาน, ค้นหาผู้สมัคร, คัดกรองเรซูเม่, ติดตามผู้สมัคร และตารางนัดสัมภาษณ์ ตามรอบ QA → Developer → QA โดยใช้ source inspection, unit tests และ Cloud E2E.

## Closed findings

- Shared shell: active navigation, mobile navigation, top actions, accessible labels และ favicon
- Dashboard/jobs: responsive content offset, search/filter controls และ CTA ซ้ำ
- Discovery: search/filter/result state, referral flow และ approve/hold/reject feedback
- Screening: upload input, tabs, target job default, AI action และ save feedback
- Applications: drawer state, responsive layout, filters และ reactive stage counts
- Interviews: board/list/month controls, date state, search, schedule action และ keyboard focus
- Authentication: logout route, session invalidation และ redirect หลังออกจากระบบ

## Deterministic evidence

| Check | Result |
|---|---|
| `npm run lint` | PASS |
| `npm run typecheck` | PASS |
| `npm test` | PASS — 82 tests |
| `npm run test:coverage` | PASS — 100% statements/branches/functions/lines |
| `npm run build` | PASS |
| Cloud E2E | PASS — 8 tests |

## Known non-blocking note

Cloud E2E ที่รันผ่าน Next dev แสดง warning ของ React เรื่อง `eval()` สำหรับ development debugging เท่านั้น โดย React ระบุว่า production mode ไม่ใช้ `eval`; ไม่พบ test failure หรือ production build failure.

## Acceptance decision

PASS สำหรับชุด acceptance ที่ตรวจได้ในรอบนี้. ห้ามตีความว่าเป็นการรับรอง production observability หรือ penetration test เต็มรูปแบบ.
