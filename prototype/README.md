# Low-Fi Clickable Prototype

Standalone HTML/CSS/JS prototype for validating TalentFlow HR flows.

Open `index.html` in a browser. No build step or backend is required.

Use **Reset fixture** to return to deterministic initial state.
